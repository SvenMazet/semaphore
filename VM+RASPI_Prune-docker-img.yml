---
- name: Remove unused docker images
  hosts: DOCKER
  become: true
  tasks:
    - name: Remove unused docker images
      shell: docker image prune -y
      register: result

    - name: Show results
      debug:
        var: result.stdout
